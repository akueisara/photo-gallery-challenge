<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="com.example.photogallerychallenge.util.Converter"/>

        <variable
            name="databasePhoto"
            type="com.example.photogallerychallenge.data.database.DatabasePhoto" />

        <variable
            name="viewmodel"
            type="com.example.photogallerychallenge.ui.photos.PhotosViewModel" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:padding="16dp">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    app:roundImageUrl="@{photo.user.profile_image.small}"
                    tools:src="@android:drawable/star_big_off" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:fontFamily="@font/roboto_medium"
                    android:text="@{photo.user.name}"
                    android:textColor="#111111"
                    android:textSize="18sp"
                    tools:text="Kuei Jung Hu" />

            </LinearLayout>

            <TextView
                android:id="@+id/user_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="16dp"
                android:fontFamily="@font/roboto"
                android:textColor="#767676"
                android:text="@{databasePhoto.description}"
                android:visibility="@{databasePhoto.description.length() == 0 ? View.GONE : View.VISIBLE}"
                tools:text="Kuei Jung Hu" />

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="400dp"
                android:scaleType="centerCrop"
                app:backgroundHexColor="@{databasePhoto.color}"
                app:imageUrl="@{databasePhoto.urls.regular}"
                tools:src="@tools:sample/avatars"
                android:onClick="@{() -> viewmodel.openPhoto(databasePhoto.id)}"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</layout>